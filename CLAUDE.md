# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業する際の指針をまとめたものです。

## 作業の基本ルール

### 読んでいないコードは変更しない

- **必ず該当ファイルを Read してから編集すること。** 推測や過去の記憶だけでコードを書き換えてはいけない。
- 修正対象の関数・クラスだけでなく、呼び出し元・呼び出し先も必要に応じて読み、影響範囲を把握してから編集する。
- 「たぶんこうなっているはず」で Edit/Write を行わない。確認せずに推測で書いた変更は、バグや退行の原因になる。

### 調査を省略せず、十分に考えてから編集する

- 複雑なタスクほど、いきなり編集に飛びつかず、まず関連コードを調査し、現状の構造と意図を理解してから手を動かす。
- タスクが曖昧な場合や、複数の解釈ができる場合は、推測で進めず必要に応じて確認する。
- 単純な置換で済むのか、設計レベルで考える必要があるのかを最初に判断する。

### 変更の範囲を広げない

- 依頼されていない修正（リファクタ、コメント追加、フォーマット変更など）を勝手に行わない。
- バグ修正のついでに周辺コードを「きれいに」しない。依頼されたスコープ内に留める。

### 根本原因を直す

- エラーやテスト失敗を、チェックを無効化したり `--no-verify` で回避したりして「消す」のではなく、原因を特定して直す。
- 想定外のファイルや状態に遭遇したら、削除・上書きの前にまず調査する。

## このリポジトリについて

台風（taiga）シミュレーション関連の Python プロジェクト。主要スクリプトはリポジトリルートの `generate_*.py` / `run_*.py`、コアロジックは `taiga_sim/` 配下、テストは `tests/` にある。
